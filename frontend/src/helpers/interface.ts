export interface FormDetailTypes {
  email: string
  password: string
}
export interface UserDataTypes {
  tableData: [{
    base64String: string
    id: string
    status: string
    path: string
  }]
 
}